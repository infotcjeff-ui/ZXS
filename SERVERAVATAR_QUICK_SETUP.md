# ServerAvatar 快速設置（簡化版）

## 🎯 關鍵設置（必須設置）

在 ServerAvatar 控制台中，找到構建設置並輸入以下值：

### Build Command（構建命令）
```
npm install && npm run build:serveravatar
```

### Output Directory（輸出目錄）⚠️ 最重要！
```
dist
```
**必須是 `dist`，不能是 `.` 或 `src`！**

### Node Version（Node 版本）
```
20
```

## 📍 在哪裡找到這些設置？

1. **在 Git 頁面**：向下滾動，查找 "Build Settings"
2. **左側導航**：查找 "Build Settings" 或 "Deployment"
3. **應用設置**：點擊應用名稱旁邊的 Expand 按鈕

## ✅ 設置完成後

1. 點擊 **Save** 保存
2. 點擊 **Deploy** 或 **Redeploy** 觸發部署
3. 等待 2-5 分鐘
4. 訪問 `https://jeff-zxs-sss.ooooo.one/` 驗證

## 🔍 驗證是否正確

打開瀏覽器開發者工具（F12），查看 Network 標籤：
- ✅ 看到 `/assets/index-xxxxx.js` = 正確
- ❌ 看到 `/src/main.jsx` = 錯誤（需要修正 Output Directory）

