# ServerAvatar 快速修復指南

## ⚠️ 當前問題

錯誤顯示 `main.jsx:1` 被引用，這表示：
- ❌ ServerAvatar 正在提供**源文件**而不是**構建後的文件**
- ❌ 構建可能沒有執行或失敗
- ❌ 輸出目錄可能設置錯誤

## ✅ 立即修復步驟

### 步驟 1: 檢查 ServerAvatar 構建設置

登入 ServerAvatar 控制台，進入您的應用設置，確認以下設置：

#### 構建命令（Build Command）
```
npm install && npm run build:serveravatar
```

#### 輸出目錄（Output Directory）
```
dist
```
⚠️ **必須是 `dist`，不能是：**
- ❌ `src`
- ❌ `.`
- ❌ `./`
- ❌ 空白

#### Node 版本（Node Version）
```
20
```
或
```
20.x
```

### 步驟 2: 檢查構建日誌

在 ServerAvatar 控制台查看最近的構建日誌，應該看到：

✅ **成功的構建日誌應該包含：**
```
> serveravatar-git@0.0.0 build:serveravatar
> vite build --config vite.config.serveravatar.js

vite v7.2.7 building client environment for production...
transforming...
✓ 58 modules transformed.
rendering chunks...
dist/index.html                   3.76 kB │ gzip:  1.76 kB
dist/assets/index-xxxxx.css      29.97 kB │ gzip:  5.95 kB
dist/assets/index-xxxxx.js       326.06 kB │ gzip: 96.48 kB
✓ built in 1.97s
✓ Copied .htaccess to dist
```

❌ **如果看到錯誤或沒有這些輸出：**
- 構建可能失敗了
- 檢查 Node 版本是否正確
- 檢查構建命令是否正確

### 步驟 3: 驗證部署的文件

構建完成後，ServerAvatar 應該部署 `dist` 目錄中的文件。

**正確的部署應該包含：**
- `index.html`（在根目錄）
- `assets/index-xxxxx.js`（構建後的 JavaScript）
- `assets/index-xxxxx.css`（構建後的 CSS）
- `.htaccess`（MIME 類型配置）

**錯誤的部署（如果看到這些，說明設置錯誤）：**
- `src/main.jsx`（源文件，不應該被部署）
- `package.json`（項目文件，不應該被部署）

### 步驟 4: 觸發重新部署

1. 在 ServerAvatar 控制台找到「**Redeploy**」或「**Deploy**」按鈕
2. 點擊觸發重新部署
3. 等待構建完成（通常 2-5 分鐘）
4. 查看構建日誌確認成功

### 步驟 5: 驗證修復

部署完成後：

1. **清除瀏覽器緩存**
   - Windows: `Ctrl + Shift + Delete`
   - Mac: `Cmd + Shift + Delete`

2. **硬刷新頁面**
   - Windows: `Ctrl + F5`
   - Mac: `Cmd + Shift + R`

3. **打開開發者工具**（F12）

4. **檢查 Network 標籤**
   - 應該看到 `index-xxxxx.js` 被載入
   - 不應該看到 `main.jsx`

5. **檢查 Console 標籤**
   - 應該不再有 MIME 類型錯誤
   - 應該看到應用正常載入

## 🔍 如何確認問題

### 檢查 1: 查看網頁源代碼

1. 訪問您的網站
2. 右鍵點擊頁面 → 「查看網頁源代碼」
3. 查找 `<script>` 標籤

✅ **正確的（構建後）：**
```html
<script type="module" crossorigin src="/assets/index-BH8yhKha.js"></script>
```

❌ **錯誤的（源文件）：**
```html
<script type="module" src="/src/main.jsx"></script>
```

### 檢查 2: 查看 Network 請求

1. 打開開發者工具（F12）
2. 切換到「Network」標籤
3. 刷新頁面
4. 查看載入的文件

✅ **應該看到：**
- `index.html`
- `assets/index-xxxxx.js`
- `assets/index-xxxxx.css`

❌ **不應該看到：**
- `main.jsx`
- `App.jsx`
- 其他 `src/` 目錄中的文件

## 🆘 如果問題仍然存在

### 選項 1: 檢查 ServerAvatar 構建日誌

1. 在 ServerAvatar 控制台找到「Build Logs」或「Deployment Logs」
2. 複製完整的構建日誌
3. 檢查是否有錯誤信息

### 選項 2: 聯繫 ServerAvatar 支持

如果構建日誌顯示成功，但網站仍然載入源文件，請聯繫 ServerAvatar 支持：

**提供以下信息：**
1. 應用名稱/URL
2. 構建日誌截圖
3. 瀏覽器控制台錯誤截圖
4. 說明：
   - 構建命令：`npm install && npm run build:serveravatar`
   - 輸出目錄：`dist`
   - 問題：部署的是源文件而不是構建後的文件

**要求他們檢查：**
- 構建是否正確執行
- 輸出目錄設置是否正確
- 為什麼部署了源文件而不是 `dist` 目錄

### 選項 3: 手動上傳構建文件

如果 Git 部署持續有問題，可以嘗試手動上傳：

1. **在本地構建：**
   ```bash
   npm run build:serveravatar
   ```

2. **檢查 `dist` 目錄：**
   - 確認 `index.html` 存在
   - 確認 `assets/` 目錄存在
   - 確認 `.htaccess` 存在

3. **在 ServerAvatar 控制台：**
   - 找到「File Manager」或「Upload Files」選項
   - 上傳 `dist` 目錄中的所有文件到網站根目錄

## 📋 檢查清單

部署前確認：

- [ ] 構建命令是 `npm install && npm run build:serveravatar`
- [ ] 輸出目錄是 `dist`（不是 `src` 或其他）
- [ ] Node 版本是 `20` 或 `20.x`
- [ ] 構建日誌顯示成功
- [ ] 構建日誌顯示 `dist/index.html` 和 `dist/assets/` 文件
- [ ] 部署後，網頁源代碼顯示 `/assets/index-xxxxx.js`（不是 `/src/main.jsx`）
- [ ] 瀏覽器 Network 標籤顯示構建後的文件（不是源文件）

## 💡 關鍵要點

**最重要的設置：**
1. **輸出目錄必須是 `dist`**
   - 這是構建後的文件所在位置
   - 如果設置為 `src` 或其他，會部署源文件

2. **構建命令必須包含 `build:serveravatar`**
   - 這會使用正確的配置（base: `/`）
   - 如果使用 `build`，會使用 GitHub Pages 配置（base: `/ZXS/`）

3. **構建必須成功執行**
   - 檢查構建日誌確認沒有錯誤
   - 確認看到 `✓ built` 消息

